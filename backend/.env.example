# ⚠️ IMPORTANTE: TODOS deben usar estos MISMOS valores
# Copiar este archivo como .env en cada servicio

# Stage - TODOS usan 'dev' en sus cuentas individuales
STAGE=dev

# AWS Profile - TODOS usan 'fridays-dev'
AWS_PROFILE=fridays-dev

# AWS Region - TODOS usan 'us-east-1'
AWS_REGION=us-east-1

# JWT Secret - MISMO para todos (para que los tokens funcionen entre servicios)
JWT_SECRET=fridays-secret-key-2025-proyectofinal

# DynamoDB Endpoint (solo para local)
DYNAMODB_ENDPOINT=http://localhost:8000

# ============================================
# Copiar este archivo como .env en:
# - services/ecommerce-service/.env
# - services/kitchen-service/.env
# - services/delivery-service/.env
# - services/admin-service/.env
# - services/websocket-service/.env
# ============================================

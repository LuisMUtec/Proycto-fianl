version: 1
frontend:
  phases:
    preBuild:
      commands:
        - cd frontend-admin
        - npm ci
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: frontend-admin/dist
    files:
      - '**/*'
  cache:
    paths:
      - frontend-admin/node_modules/**/*
